name: Validate

on:
  release:
    types: [published]

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: HACS Validate
        uses: hacs/action@main
        with:
          category: lovelace